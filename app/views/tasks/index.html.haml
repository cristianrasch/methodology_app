= content_for :title, "#{Task.model_name.human.humanize.pluralize}. #{@project}"
= content_for :sidebar, render('sidebar')

- if @tasks.empty?
  = not_found(Task)
- else
  = render 'list', :list => @tasks
