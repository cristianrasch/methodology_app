#menu
  %ul
    - if user_signed_in?
      %li.current_page_item.first
        = link_to Project.model_name.human.humanize.pluralize, projects_path
      /
        %li
          = link_to Holiday.model_name.human.humanize.pluralize, admin_holidays_path
      %li
        = link_to OrgUnit.model_name.human.humanize.pluralize, org_units_path
      %li
        = link_to 'Salir', destroy_user_session_path
      %li.current_page_item.first.right
        = link_to current_user.username, edit_account_path
    - else
      %li.current_page_item.first
        = link_to 'Ingresar', new_user_session_path
/ end #menu
