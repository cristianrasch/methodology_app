%li 
  [#{l(holiday.date)}] - #{holiday.name}
  = link_to(image_tag("edit.png", :border => 0), edit_holiday_path(holiday))
  &nbsp;|&nbsp;
  = link_to(image_tag("delete.png", :border => 0), holiday_path(holiday), 
            :method => :delete, :confirm => "¿Está seguro que desea eliminar el #{Holiday.model_name.human}?")
