= content_for :title, "Nuevo #{Event.model_name.human} - #{@project.to_s(:short)}"
= content_for :sidebar do
  = render 'sidebar'
  
= form_for [@project, @event], :html => {:multipart => true} do |f|
  = render f
  = f.submit 'Crear'
