= content_for :title, "#{Event.model_name.human.humanize.pluralize} del #{Project.model_name.human} #{@project}"
= content_for :sidebar do
  = render 'sidebar'

- if @events.empty?
  = not_found(Event)
- else
  = render 'list', :list => @events
