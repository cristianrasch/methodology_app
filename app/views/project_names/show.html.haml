= content_for :title, @project_name.text
= content_for :sidebar do
  = render 'sidebar'

%p
  %strong= ProjectName.human_attribute_name(:path)
  %br/
  = path_for(@project_name)

= created_at(@project_name)
= updated_at(@project_name)