= content_for :title, "Editando #{Comment.model_name.human}"
= content_for :sidebar do
  = render 'sidebar'
  
= form_for @comment, :html => {:multipart => true} do |f|
  = render f
  = f.submit 'Actualizar'
  
