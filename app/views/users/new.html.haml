= content_for :sidebar, render('sidebar')

= form_for @user do |f|
  %p
    = f.label :username
    %br/
    = text_field_tag :username, @user.username, :size => 30
  
  = render f
  = f.submit 'Crear'
