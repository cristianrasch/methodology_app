= content_for :sidebar, render('sidebar')

= form_for @user do |f|
  = f.error_messages :header_message => nil, :message => nil
  
  %p
    = f.label :username
    %br/
    = text_field_tag :username, @user.username, :size => 30
  
  = render f
  = f.submit 'Crear'
